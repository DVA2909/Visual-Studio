//#include <iomanip>
//#include <cmath>
//#include <iostream>
//using namespace std;

//void main()
//{
	/*
	int n;
	cout << "n : "; cin >> n;
	double y;
	y = (((cbrt(n - 1) * (double)log(pow(2, n))) - ((double)tan((n + 1) / pow(n, 3)))) / ((double)acos(0.85 + log(n)))) + 2.75;
	cout << "resultat : " << y << endl;
	*/




	/*
	float x = 0.6, s = 5.6;
	double r, r1;
	r = sin(x) + log(s);
	r1 = sqrt(pow(r, 3));
	cout << "x : " << x << endl;
	cout << "s : " << s << endl;
	cout << "r : " << r << endl;
	cout << "r1 : " << r1 << endl;
	*/



	
	/*
	const int size = 6;
	int matrix[size];
	int min_chislo;
	int suma_chisel = 0;
	for (int i = 0; i < size; i++)
	{
		cout << "Enter " << i + 1 << " number : "; cin >> matrix[i];
	}
	min_chislo = matrix[0];
	cout << endl;
	for (int i = 0; i < size; i++)
	{
		cout << matrix[i] << ' ';
		suma_chisel += matrix[i];
		if (matrix[i] < min_chislo) min_chislo = matrix[i];
	}
	cout << endl;
	cout << "Minimalne chislo : " << min_chislo << endl;
	cout << "Suma chisel : " << suma_chisel << endl;
	*/
//}